import styled from 'styled-components';

export const SwitchThemeWrapper = styled.div`
  position: absolute;
  top: 18px;
  right: 0;
  display: flex;
  span {
    color: white;
    margin: 0 10px;
  }
`;
